import Foo.{Bar, Baz};

// functions with no return type are void by default
function main() {
    Console.log("Hello, world!");
}

class Foo {
    function bar() {
        Console.log("Hello, world!");
    }
    let baz: u8 = 10 * 1 + (1 + 1);
}